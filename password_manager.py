pwd = input("What is the master password? ")

while True:
    mode = input("Would ou like to add a new password or view existing ones ('view' or 'add') ")
    if mode == "view":
        pass
    elif mode == "add":
        pass
    print("Invalid answer.")
